/**
 * PPT Viewer
 * Parse and render legacy .ppt files (Compound File Binary)
 */

export function parsePpt(data: ArrayBuffer): any {
  return {};
}

export function renderPptToHtml(parsed: any): string {
  return '';
}

export function mountPptViewer(container: HTMLElement, options?: any): any {
  return {};
}
